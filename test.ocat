mut float x = float (2147483648)
mut uint y = x + 2
y = float (y)
int spd = fetch (speed, speed > 20 and speed < 50) with timeout rel 3 + x
if timeout then
    send (cmd, false)
else
    send (cmd, true)
end if
